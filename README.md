# Pordier at War Wiki Source
Source code for the semi-official wiki

## Contributing
> Requires Ruby 3.1.x, preferably 3.1.7

1. Install Ruby
2. Install bundler with `gem install jekyll bundler`
3. Install dependencies with `bundle install`
4. Build and run Jekyll locally with `bundle exec jekyll serve`
